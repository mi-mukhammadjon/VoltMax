# Versiyalar QATTIQ qadalgan.
#
# Ilgari hamma joyda `>=` turardi: bugun ishlaydigan kod ertaga Railway
# yangi versiyani o'rnatgach buzilishi mumkin edi va buni oldindan bilib
# bo'lmasdi. Endi lokal muhit, CI va server bir xil versiyalarni oladi.
#
# Yangilash tartibi: versiyani qo'lda ko'taring, testlarni to'liq
# o'tkazing, keyin commit qiling. Bir vaqtda bittadan yangilang — nima
# buzganini shunda ko'rish oson.

# --- Asosiy ------------------------------------------------------
Django==5.2.17
asgiref==3.9.1
sqlparse==0.6.0

# --- REST API ----------------------------------------------------
djangorestframework==3.17.2
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
requests==2.34.2

# --- OCPP (WebSocket) --------------------------------------------
channels==4.3.2
daphne==4.2.3

# --- Baza va statik fayllar --------------------------------------
dj-database-url==3.1.2
psycopg2-binary==2.9.12
whitenoise==6.12.0
gunicorn==26.0.0

# --- Fayllar va hujjatlar ----------------------------------------
pillow==12.3.0
python-docx==1.2.0
lxml==6.1.2

# --- Sozlamalar --------------------------------------------------
python-dotenv==1.2.2

# --- Faqat serverda kerak bo'ladiganlar --------------------------
# Bular lokal muhitda o'rnatilmagan (R2 va Sentry ishlab chiqishda
# ishlatilmaydi), shuning uchun aniq versiya sinovdan o'tkazilmagan.
# Katta versiya o'zgarishidan himoya qilish uchun yuqori chegara bor.
django-storages[s3]>=1.14,<2.0
websockets>=12.0,<16.0

# Ixtiyoriy: SENTRY_DSN berilgandagina ishlaydi
sentry-sdk>=2.0,<3.0

# Ikki bosqichli kirishni sozlashda QR kod (SVG). Ixtiyoriy: bo'lmasa
# kalit qo'lda kiritiladi, sahifa baribir ishlaydi.
qrcode>=7.4,<9.0
